@model IEnumerable<DichVu>
@await Html.PartialAsync("header")
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Quản lý dịch vụ</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 900px;
            margin: 40px auto;
            background-color: #fff;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 0 12px rgba(0,0,0,0.1);
        }

        h1 {
            text-align: center;
            color: white;
            background-color: #007BFF;
        @* Padding trên/dưới 15px, trái/phải 0 *@
            padding: 15px 0;
        @* Margin âm để kéo ra ngoài container, dưới 20px *@
            margin: -30px -30px 20px -30px;
            border-radius: 12px 12px 0 0;
        }

        .error {
            color: red;
            text-align: center;
            margin-bottom: 10px;
        }

        .btn {
            background-color: #007BFF;
            color: white;
        @* Không viền *@
            border: none;
            padding: 10px 18px;
            border-radius: 6px;
            cursor: pointer;
            margin: 4px 0;
        @* Hiệu ứng chuyển màu *@
            transition: background 0.3s;
        }

            .btn:hover {
                background-color: #0056b3;
            }

        .btn-danger {
            background-color: #dc3545;
        }

            .btn-danger:hover {
                background-color: #a71d2a;
            }

        table {
            width: 100%;
        @* Gộp viền bảng lại với nhau *@
            border-collapse: collapse;
            margin-top: 15px;
        }

        th, td {
            border: 1px solid #ccc;
            padding: 12px;
            text-align: center;
        }

        th {
            background-color: #007BFF;
            color: white;
        }

        /* Modal */
        .modal {
        @* Ẩn modal ban đầu *@
            display: none;
        @* Vị trí cố định trên màn hình *@
            position: fixed;
        @* Lớp cao nhất để hiển thị trên tất cả element khác *@
            z-index: 9999;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
        @* Màu nền đen mờ 40% để tạo overlay *@
            background-color: rgba(0,0,0,0.4);
        }

        .modal-content {
            background-color: #fff;
            margin: 10% auto;
            padding: 20px 30px;
            border-radius: 10px;
            width: 400px;
        @* Vị trí relative để chứa absolute elements *@
            position: relative;
            box-shadow: 0 0 10px rgba(0,0,0,0.3);
        }

        .close {
        @* Vị trí tuyệt đối trong modal-content *@
            position: absolute;
            top: 10px;
            right: 15px;
            font-size: 24px;
            color: #aaa;
            cursor: pointer;
        }

            .close:hover {
                color: #000;
            }

        .form-group {
        @* Khoảng cách dưới 15px giữa các nhóm form *@
            margin-bottom: 15px;
        }

        label {
        @* Hiển thị dạng block (xuống dòng) *@
            display: block;
        @* Khoảng cách dưới 6px *@
            margin-bottom: 6px;
        @* Chữ đậm cho label *@
            font-weight: bold;
        }

        input[type="text"],
        input[type="number"] {
            width: 100%;
            padding: 8px 10px;
            border: 1px solid #ccc;
            border-radius: 6px;
        @* Tính padding và border vào tổng kích thước *@
            box-sizing: border-box;
        }
    </style>
    <script>
        @* Hàm JavaScript mở modal theo ID *@
        function openModal(id) {
        @* Tìm element theo ID và hiển thị modal (display = block) *@
            document.getElementById(id).style.display = 'block';
        }
        function closeModal(id) {
        @* Tìm element theo ID và ẩn modal (display = none) *@
            document.getElementById(id).style.display = 'none';
        }
    </script>
</head>
<body>
    <div class="container">
        @if (ViewBag.error == false)
        {
            <div class="error">Trùng mã dịch vụ, vui lòng chọn mã khác!</div>
        }

        <h1>Quản lý dịch vụ</h1>

        @* Button mở modal thêm dịch vụ, gọi hàm openModal với ID 'addModal' *@
        <button class="btn" onclick="openModal('addModal')">+ Thêm dịch vụ</button>

        <!-- Modal Thêm -->
        <div class="modal" id="addModal">
            <div class="modal-content">
                @* Nút đóng modal (ký tự × - HTML entity) *@
                <span class="close" onclick="closeModal('addModal')">&times;</span>
                <h3>Thêm dịch vụ</h3>
                @* Form POST đến action 'themDichVu' *@
                <form method="post" asp-action="themDichVu">
                    <div class="form-group">
                        <label>Mã dịch vụ</label>
                        @* Input text cho tên dịch vụ, bắt buộc nhập *@
                        <input name="madichvu" type="text" required />
                    </div>
                    <div class="form-group">
                        <label>Tên dịch vụ</label>
                        <input name="tendichvu" type="text" required />
                    </div>
                    <div class="form-group">
                        <label>Giá dịch vụ</label>
                        <input name="giadichvu" type="number" required />
                    </div>
                    <button type="submit" class="btn btn-danger">Xác nhận</button>
                </form>
            </div>
        </div>

        <!-- Bảng -->
        <table>
            <thead>
                <tr>
                    <th>STT</th>
                    <th>Mã dịch vụ</th>
                    <th>Tên dịch vụ</th>
                    <th>Giá dịch vụ</th>
                    <th>Sửa</th>
                    <th>Xóa</th>
                </tr>
            </thead>
            <tbody>
                @{
                    int i = 1;
                    @* Vòng lặp qua từng dịch vụ trong Model *@
                    foreach (var dv in Model)
                    {
                        <tr>
                            <td>@(i++)</td> @* Hiển thị STT và tăng biến đếm *@
                            <td>@dv.MaDichVu</td>
                            <td>@dv.TenDichVu</td>

                            @* Hiển thị giá dịch vụ với định dạng số có dấu phẩy *@
                            <td>@String.Format("{0:N0}", dv.GiaDichVu)</td>
                            <td>
                                @* Button sửa, mở modal với ID động 'edit_[MaDichVu]' *@
                                <button class="btn" onclick="openModal('edit_@dv.MaDichVu')">Sửa</button>

                                <!-- Modal sửa -->
                                @* Modal sửa với ID động cho từng dịch vụ *@
                                <div class="modal" id="edit_@dv.MaDichVu">
                                    <div class="modal-content">
                                        <span class="close" onclick="closeModal('edit_@dv.MaDichVu')">&times;</span>
                                        <h3>Sửa dịch vụ</h3>

                                        @* Form POST đến action 'updateDichVu' *@
                                        <form method="post" asp-action="updateDichVu">
                                            <div class="form-group">
                                                <label>Mã dịch vụ</label>
                                                @* Input readonly (chỉ đọc) cho mã dịch vụ, không cho phép sửa *@
                                                <input name="madichvu" value="@dv.MaDichVu" readonly />
                                            </div>
                                            <div class="form-group">
                                                <label>Tên dịch vụ</label>
                                                <input name="tendichvu" value="@dv.TenDichVu" required />
                                            </div>
                                            <div class="form-group">
                                                <label>Giá dịch vụ</label>
                                                <input name="giadichvu" type="number" value="@dv.GiaDichVu" required />
                                            </div>
                                            <button type="submit" class="btn btn-danger">Xác nhận</button>
                                        </form>
                                    </div>
                                </div>
                            </td>
                            <td>
                                <button class="btn btn-danger" onclick="openModal('delete_@dv.MaDichVu')">Xóa</button>

                                <!-- Modal xóa -->
                                @* Modal xác nhận xóa với ID động *@
                                <div class="modal" id="delete_@dv.MaDichVu">
                                    <div class="modal-content">
                                        @* Nút đóng modal xóa *@
                                        <span class="close" onclick="closeModal('delete_@dv.MaDichVu')">&times;</span>
                                        <h3>Xóa dịch vụ</h3>
                                        <p>Bạn có chắc muốn xóa dịch vụ này không?</p>
                                        @* Link xóa đến action 'xoaDichVu' với parameter madichvu *@
                                        <a href="@Url.Action("xoaDichVu", new { madichvu = dv.MaDichVu })" class="btn btn-danger">Xóa</a>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</body>
</html>
